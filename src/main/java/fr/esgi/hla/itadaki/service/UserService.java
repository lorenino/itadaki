package fr.esgi.hla.itadaki.service;

import fr.esgi.hla.itadaki.dto.user.UserResponseDto;

/**
 * TODO: Defines user profile operations.
 *       - findById(Long id)           → UserResponseDto
 *       - findByEmail(String email)   → UserResponseDto
 *       - updateProfile(Long id, ...) → UserResponseDto
 *       - deleteUser(Long id)         → void (admin use)
 */
public interface UserService {

    // TODO: UserResponseDto findById(Long id);
    // TODO: UserResponseDto findByEmail(String email);
}
